# 登入 / 跨裝置同步 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 加入 Google OAuth 登入，登入後答題歷史同步 Supabase，未登入看不了熱力圖。

**Architecture:** Supabase Auth 處理 Google OAuth，新建 `user_answers` 表（RLS 保護）儲存登入使用者的答題紀錄。前端用 `useUser` hook 監聽登入狀態，`LoginGate` 元件控制熱力圖可見性，`AuthButton` 放在 Navbar。

**Tech Stack:** Next.js 16, Supabase (@supabase/ssr ^0.9, @supabase/supabase-js ^2), TypeScript, React

---

## 檔案結構

| 動作 | 路徑 | 職責 |
|------|------|------|
| 新增 | `src/hooks/useUser.ts` | 監聽 Supabase auth 狀態，回傳 `User \| null` |
| 新增 | `src/components/ui/AuthButton.tsx` | 登入/登出按鈕 |
| 新增 | `src/components/ui/LoginGate.tsx` | 熱力圖登入遮罩 |
| 新增 | `src/app/auth/callback/route.ts` | OAuth code exchange handler |
| 修改 | `src/components/layout/Navbar.tsx` | 加入 `<AuthButton />` |
| 修改 | `src/components/results/ResultsClient.tsx` | 熱力圖改用 `<LoginGate>` |
| 修改 | `src/app/api/submit-answer/route.ts` | 有 session 時 upsert `user_answers` |
| 修改 | `src/app/history/page.tsx` | 登入後從 Supabase 讀歷史 |
| 修改 | `src/components/question/QuestionCard.tsx` | 登入後不寫 localStorage |

---

## Task 1：建立 Supabase `user_answers` 表

**Files:**
- 手動執行 SQL（Supabase console → SQL Editor）

- [ ] **Step 1: 在 Supabase SQL Editor 執行以下 SQL**

```sql
create table user_answers (
  id           uuid primary key default gen_random_uuid(),
  user_id      uuid references auth.users not null,
  question_id  uuid not null,
  date         text not null,
  text_zh      text not null,
  user_answer  text not null,
  answered_at  timestamptz not null default now(),
  unique(user_id, question_id)
);

alter table user_answers enable row level security;

create policy "users see own" on user_answers
  for all using (auth.uid() = user_id);
```

- [ ] **Step 2: 確認表已建立**

在 Supabase console → Table Editor，確認 `user_answers` 出現，且有 RLS 標記。

---

## Task 2：設定 Google OAuth Provider

**Files:**
- 無程式碼，僅 Supabase console 設定

- [ ] **Step 1: 在 Supabase console 開啟 Google OAuth**

前往 Authentication → Providers → Google，輸入 Google OAuth Client ID 和 Secret（從 Google Cloud Console 取得），開啟 Enable。

- [ ] **Step 2: 確認 Redirect URL**

在同一頁面確認 Callback URL 為：`https://<your-project>.supabase.co/auth/v1/callback`

將此 URL 加到 Google Cloud Console → OAuth 2.0 → Authorized redirect URIs。

- [ ] **Step 3: 設定 Site URL**

在 Supabase console → Authentication → URL Configuration，設定：
- Site URL: `http://localhost:3000`（本機開發）
- Additional Redirect URLs: `http://localhost:3000/auth/callback`

---

## Task 3：新增 OAuth callback route

**Files:**
- Create: `src/app/auth/callback/route.ts`

- [ ] **Step 1: 建立 callback route**

```typescript
// src/app/auth/callback/route.ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
import { NextRequest, NextResponse } from 'next/server'

export async function GET(request: NextRequest) {
  const { searchParams, origin } = new URL(request.url)
  const code = searchParams.get('code')
  const next = searchParams.get('next') ?? '/'

  if (code) {
    const cookieStore = await cookies()
    const supabase = createServerClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL!,
      process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
      {
        cookies: {
          getAll() {
            return cookieStore.getAll()
          },
          setAll(cookiesToSet) {
            cookiesToSet.forEach(({ name, value, options }) =>
              cookieStore.set(name, value, options)
            )
          },
        },
      }
    )
    const { error } = await supabase.auth.exchangeCodeForSession(code)
    if (!error) {
      return NextResponse.redirect(`${origin}${next}`)
    }
  }

  return NextResponse.redirect(`${origin}/`)
}
```

- [ ] **Step 2: Commit**

```bash
git add src/app/auth/callback/route.ts
git commit -m "feat: add OAuth callback route"
```

---

## Task 4：新增 `useUser` hook

**Files:**
- Create: `src/hooks/useUser.ts`

- [ ] **Step 1: 建立 hook**

```typescript
// src/hooks/useUser.ts
'use client'

import { useEffect, useState } from 'react'
import type { User } from '@supabase/supabase-js'
import { createClient } from '@/lib/supabase/client'

export function useUser(): User | null {
  const [user, setUser] = useState<User | null>(null)

  useEffect(() => {
    const supabase = createClient()

    // 初始取得 session，避免畫面 flash
    supabase.auth.getSession().then(({ data: { session } }) => {
      setUser(session?.user ?? null)
    })

    // 監聽 auth 狀態變化
    const { data: { subscription } } = supabase.auth.onAuthStateChange(
      (_event, session) => {
        setUser(session?.user ?? null)
      }
    )

    return () => subscription.unsubscribe()
  }, [])

  return user
}
```

- [ ] **Step 2: Commit**

```bash
git add src/hooks/useUser.ts
git commit -m "feat: add useUser hook for Supabase auth state"
```

---

## Task 5：新增 `AuthButton` 元件

**Files:**
- Create: `src/components/ui/AuthButton.tsx`

- [ ] **Step 1: 建立元件**

```typescript
// src/components/ui/AuthButton.tsx
'use client'

import { createClient } from '@/lib/supabase/client'
import { useUser } from '@/hooks/useUser'

export function AuthButton() {
  const user = useUser()
  const supabase = createClient()

  async function handleLogin() {
    await supabase.auth.signInWithOAuth({
      provider: 'google',
      options: {
        redirectTo: `${window.location.origin}/auth/callback`,
      },
    })
  }

  async function handleLogout() {
    await supabase.auth.signOut()
  }

  if (user) {
    // 顯示 email 前綴（ayaka@example.com → ayaka）
    const displayName = user.email?.split('@')[0] ?? '使用者'
    return (
      <button
        onClick={handleLogout}
        style={{
          background: 'none',
          border: 'none',
          cursor: 'pointer',
          color: 'var(--text-muted)',
          fontSize: 14,
          padding: 0,
        }}
      >
        {displayName} · 登出
      </button>
    )
  }

  return (
    <button
      onClick={handleLogin}
      style={{
        background: 'none',
        border: 'none',
        cursor: 'pointer',
        color: 'var(--text-muted)',
        fontSize: 14,
        padding: 0,
      }}
    >
      登入
    </button>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/ui/AuthButton.tsx
git commit -m "feat: add AuthButton component for Google OAuth"
```

---

## Task 6：新增 `LoginGate` 元件

**Files:**
- Create: `src/components/ui/LoginGate.tsx`

- [ ] **Step 1: 建立元件**

```typescript
// src/components/ui/LoginGate.tsx
'use client'

import type { ReactNode } from 'react'
import { createClient } from '@/lib/supabase/client'
import { useUser } from '@/hooks/useUser'

interface LoginGateProps {
  children: ReactNode
}

export function LoginGate({ children }: LoginGateProps) {
  const user = useUser()

  if (user) return <>{children}</>

  async function handleLogin() {
    const supabase = createClient()
    await supabase.auth.signInWithOAuth({
      provider: 'google',
      options: {
        redirectTo: `${window.location.origin}/auth/callback`,
      },
    })
  }

  return (
    <div style={{ position: 'relative' }}>
      {/* 模糊底層 */}
      <div style={{ filter: 'blur(6px)', opacity: 0.3, pointerEvents: 'none' }}>
        {children}
      </div>

      {/* 遮罩卡片 */}
      <div
        style={{
          position: 'absolute',
          inset: 0,
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          gap: 16,
        }}
      >
        <p style={{ fontSize: 15, color: 'var(--text)', fontWeight: 600, margin: 0 }}>
          登入以查看熱力圖
        </p>
        <button
          onClick={handleLogin}
          style={{
            padding: '10px 24px',
            borderRadius: 8,
            border: 'none',
            background: 'var(--teal)',
            color: '#fff',
            fontSize: 14,
            fontWeight: 600,
            cursor: 'pointer',
          }}
        >
          Google 登入
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/ui/LoginGate.tsx
git commit -m "feat: add LoginGate component for heatmap auth wall"
```

---

## Task 7：更新 Navbar 加入 AuthButton

**Files:**
- Modify: `src/components/layout/Navbar.tsx`

- [ ] **Step 1: 修改 Navbar**

將 `Navbar.tsx` 完整替換為：

```typescript
// src/components/layout/Navbar.tsx
import Link from 'next/link'
import { ThemeToggle } from '@/components/ui/ThemeToggle'
import { AuthButton } from '@/components/ui/AuthButton'

export function Navbar() {
  return (
    <nav
      style={{
        background: 'var(--surface)',
        borderBottom: '1px solid var(--border)',
        padding: '0 24px',
        height: 56,
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
      }}
    >
      {/* 左側：品牌名稱 */}
      <Link
        href="/"
        style={{
          color: 'var(--teal)',
          fontWeight: 700,
          fontSize: 18,
          textDecoration: 'none',
          letterSpacing: '0.02em',
        }}
      >
        人類觀測站
      </Link>

      {/* 右側：導航連結 + 登入 + 主題切換 */}
      <div style={{ display: 'flex', alignItems: 'center', gap: 16 }}>
        <Link
          href="/history"
          style={{ color: 'var(--text-muted)', textDecoration: 'none', fontSize: 14 }}
        >
          歷史紀錄
        </Link>
        <AuthButton />
        <ThemeToggle />
      </div>
    </nav>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/layout/Navbar.tsx
git commit -m "feat: add AuthButton to Navbar"
```

---

## Task 8：ResultsClient 熱力圖改用 LoginGate

**Files:**
- Modify: `src/components/results/ResultsClient.tsx`

- [ ] **Step 1: 替換 import**

找到 ResultsClient.tsx 頂部的 import，將 `PremiumGate` 改為 `LoginGate`：

```typescript
// 移除這行：
import { PremiumGate } from '@/components/ui/PremiumGate'
// 加入這行：
import { LoginGate } from '@/components/ui/LoginGate'
```

- [ ] **Step 2: 替換熱力圖包裝**

找到 WorldHeatmap 和 TimeHeatmap 的 div，改為：

```tsx
          {/* WorldHeatmap */}
          <div className="reveal-item" style={{ opacity: 0, marginTop: 32 }}>
            <LoginGate>
              <WorldHeatmap
                regionBreakdown={stats?.region_breakdown ?? {}}
                userAnswer={userAnswer}
                options={options}
              />
            </LoginGate>
          </div>

          {/* TimeHeatmap */}
          <div className="reveal-item" style={{ opacity: 0, marginTop: 24 }}>
            <LoginGate>
              <TimeHeatmap
                timeBreakdown={stats?.time_breakdown ?? {}}
                options={options}
              />
            </LoginGate>
          </div>
```

- [ ] **Step 3: Commit**

```bash
git add src/components/results/ResultsClient.tsx
git commit -m "feat: replace PremiumGate with LoginGate on heatmaps"
```

---

## Task 9：submit-answer route 寫入 user_answers

**Files:**
- Modify: `src/app/api/submit-answer/route.ts`

- [ ] **Step 1: 修改 route，加入 session 讀取和 upsert**

在 `route.ts` 中，將 `createServerClient` import 改為使用 `@supabase/ssr`，並在成功插入答案後 upsert `user_answers`。

將整個 `route.ts` 替換為：

```typescript
// src/app/api/submit-answer/route.ts
import { NextResponse } from 'next/server'
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function POST(request: Request) {
  let body: { question_id: string; value: string; date?: string; text_zh?: string }
  try {
    body = await request.json() as { question_id: string; value: string; date?: string; text_zh?: string }
  } catch {
    return NextResponse.json({ error: 'invalid JSON body' }, { status: 400 })
  }
  const { question_id, value, date, text_zh } = body

  if (!question_id || !value) {
    return NextResponse.json({ error: 'missing fields' }, { status: 400 })
  }

  const VALID_VALUES = ['A', 'B', 'C', 'D', 'E']
  if (!VALID_VALUES.includes(value)) {
    return NextResponse.json({ error: 'invalid value' }, { status: 400 })
  }

  const countryCode = request.headers.get('x-vercel-ip-country') ?? 'UNKNOWN'

  const cookieStore = await cookies()
  const supabase = createServerClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    {
      cookies: {
        getAll() {
          return cookieStore.getAll()
        },
        setAll(cookiesToSet) {
          cookiesToSet.forEach(({ name, value, options }) =>
            cookieStore.set(name, value, options)
          )
        },
      },
    }
  )

  const { error: questionError } = await supabase
    .from('questions')
    .select('id')
    .eq('id', question_id)
    .single()

  if (questionError) {
    return NextResponse.json({ error: 'invalid question_id' }, { status: 400 })
  }

  const { error: insertError } = await supabase
    .from('answers')
    .insert({ question_id, value, region: countryCode })

  if (insertError) {
    return NextResponse.json({ error: insertError.message }, { status: 500 })
  }

  // 若使用者已登入，同步寫入 user_answers
  const { data: { session } } = await supabase.auth.getSession()
  if (session?.user && date && text_zh) {
    await supabase.from('user_answers').upsert(
      {
        user_id: session.user.id,
        question_id,
        date,
        text_zh,
        user_answer: value,
        answered_at: new Date().toISOString(),
      },
      { onConflict: 'user_id,question_id', ignoreDuplicates: true }
    )
  }

  // 重新統計：distribution + region_breakdown + time_breakdown
  const { data: allAnswers, error: countError } = await supabase
    .from('answers')
    .select('value, region, created_at')
    .eq('question_id', question_id)

  if (countError) {
    return NextResponse.json({ error: countError.message }, { status: 500 })
  }

  const distribution: Record<string, number> = {}
  const regionBreakdown: Record<string, Record<string, number>> = {}
  const timeBreakdown: Record<string, Record<string, number>> = {}

  for (const row of allAnswers ?? []) {
    distribution[row.value] = (distribution[row.value] ?? 0) + 1

    if (row.region && row.region !== 'UNKNOWN') {
      regionBreakdown[row.region] ??= {}
      regionBreakdown[row.region][row.value] = (regionBreakdown[row.region][row.value] ?? 0) + 1
    }

    if (row.created_at) {
      const utc8Hour = new Date(
        new Date(row.created_at).getTime() + 8 * 60 * 60 * 1000
      ).getUTCHours()
      const hourKey = String(utc8Hour).padStart(2, '0')
      timeBreakdown[hourKey] ??= {}
      timeBreakdown[hourKey][row.value] = (timeBreakdown[hourKey][row.value] ?? 0) + 1
    }
  }

  const total_count = allAnswers?.length ?? 0

  const { data: stats, error: upsertError } = await supabase
    .from('daily_stats')
    .upsert(
      {
        question_id,
        total_count,
        distribution,
        region_breakdown: regionBreakdown,
        time_breakdown: timeBreakdown,
        updated_at: new Date().toISOString(),
      },
      { onConflict: 'question_id' }
    )
    .select()
    .single()

  if (upsertError) {
    return NextResponse.json({ error: upsertError.message }, { status: 500 })
  }

  return NextResponse.json({ success: true, stats })
}
```

- [ ] **Step 2: Commit**

```bash
git add src/app/api/submit-answer/route.ts
git commit -m "feat: upsert user_answers in submit-answer when logged in"
```

---

## Task 10：QuestionCard 送出時傳入 date/text_zh，登入後不寫 localStorage

**Files:**
- Modify: `src/components/question/QuestionCard.tsx`

- [ ] **Step 1: 修改 QuestionCard**

將 `QuestionCard.tsx` 完整替換為：

```typescript
// src/components/question/QuestionCard.tsx
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import type { Question } from '@/types'
import { saveHistory } from '@/lib/history'
import { useUser } from '@/hooks/useUser'

interface QuestionCardProps {
  question: Question
}

const ANSWER_COLOR: Record<string, string> = {
  A: 'var(--answer-a)',
  B: 'var(--answer-b)',
  C: 'var(--answer-c)',
  D: 'var(--answer-d)',
  E: 'var(--answer-e)',
}

export function QuestionCard({ question }: QuestionCardProps) {
  const router = useRouter()
  const user = useUser()
  const [selected, setSelected] = useState<string | null>(null)
  const [submitting, setSubmitting] = useState(false)
  const [error, setError] = useState<string | null>(null)

  async function handleSubmit() {
    if (!selected || submitting) return
    setSubmitting(true)
    setError(null)

    const res = await fetch('/api/submit-answer', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        question_id: question.id,
        value: selected,
        date: question.date,
        text_zh: question.text_zh,
      }),
    })

    if (!res.ok) {
      setError('送出失敗，請重試')
      setSubmitting(false)
      return
    }

    // 未登入才寫 localStorage；登入後 DB 已由 API 寫入
    if (!user) {
      saveHistory({
        question_id: question.id,
        date: question.date,
        text_zh: question.text_zh,
        userAnswer: selected,
      })
    }

    router.push(`/results/${question.id}?answer=${selected}`)
  }

  return (
    <div
      style={{
        background: 'var(--surface)',
        border: '1px solid var(--border)',
        borderRadius: 'var(--radius)',
        padding: '32px 28px',
        maxWidth: 560,
        width: '100%',
      }}
    >
      {/* 日期標籤 */}
      <div
        style={{
          fontSize: 12,
          color: 'var(--teal)',
          letterSpacing: '0.08em',
          textTransform: 'uppercase',
          marginBottom: 16,
        }}
      >
        {question.date} · 每日一題
      </div>

      {/* 題目文字 */}
      <h1
        style={{
          fontSize: 20,
          fontWeight: 600,
          lineHeight: 1.6,
          color: 'var(--text)',
          marginBottom: 28,
        }}
      >
        {question.text_zh}
      </h1>

      {/* 選項列表 */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>
        {question.options.map((opt) => {
          const isSelected = selected === opt.key
          const color = ANSWER_COLOR[opt.key] ?? 'var(--teal)'
          return (
            <button
              key={opt.key}
              onClick={() => setSelected(opt.key)}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 12,
                padding: '12px 16px',
                borderRadius: 8,
                border: `1px solid ${isSelected ? `${color}80` : 'var(--border)'}`,
                background: isSelected ? `${color}12` : 'var(--surface-high)',
                cursor: 'pointer',
                textAlign: 'left',
                transition: 'border-color 0.15s, background 0.15s',
              }}
            >
              <span
                style={{
                  width: 28,
                  height: 28,
                  borderRadius: '50%',
                  border: `1.5px solid ${isSelected ? color : 'var(--border)'}`,
                  display: 'flex',
                  alignItems: 'center',
                  justifyContent: 'center',
                  fontSize: 12,
                  fontWeight: 600,
                  color: isSelected ? color : 'var(--text-muted)',
                  flexShrink: 0,
                  transition: 'border-color 0.15s, color 0.15s',
                }}
              >
                {opt.key}
              </span>
              <span
                style={{
                  fontSize: 15,
                  color: isSelected ? 'var(--text)' : 'var(--text-muted)',
                  lineHeight: 1.5,
                }}
              >
                {opt.label_zh}
              </span>
            </button>
          )
        })}
      </div>

      {/* 錯誤訊息 */}
      {error && (
        <p style={{ marginTop: 12, color: 'var(--answer-d)', fontSize: 13 }}>
          {error}
        </p>
      )}

      {/* 提交按鈕 */}
      <button
        onClick={handleSubmit}
        disabled={!selected || submitting}
        style={{
          marginTop: 24,
          width: '100%',
          padding: '14px',
          borderRadius: 8,
          border: 'none',
          background: selected ? 'var(--teal)' : 'var(--surface-high)',
          color: selected ? '#fff' : 'var(--text-muted)',
          fontSize: 15,
          fontWeight: 600,
          cursor: selected ? 'pointer' : 'not-allowed',
          transition: 'background 0.2s, color 0.2s',
        }}
      >
        {submitting ? '送出中…' : '送出答案'}
      </button>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/question/QuestionCard.tsx
git commit -m "feat: pass date/text_zh to submit-answer, skip localStorage when logged in"
```

---

## Task 11：歷史頁登入後從 Supabase 讀

**Files:**
- Modify: `src/app/history/page.tsx`

- [ ] **Step 1: 修改 history page**

將 `history/page.tsx` 完整替換為：

```typescript
// src/app/history/page.tsx
'use client'

import { useEffect, useState } from 'react'
import { useRouter } from 'next/navigation'
import { getHistory, type HistoryEntry } from '@/lib/history'
import { useUser } from '@/hooks/useUser'
import { createClient } from '@/lib/supabase/client'

const ANSWER_COLOR: Record<string, string> = {
  A: 'var(--answer-a)',
  B: 'var(--answer-b)',
  C: 'var(--answer-c)',
  D: 'var(--answer-d)',
  E: 'var(--answer-e)',
}

export default function HistoryPage() {
  const router = useRouter()
  const user = useUser()
  const [mounted, setMounted] = useState(false)
  const [entries, setEntries] = useState<HistoryEntry[]>([])

  useEffect(() => {
    setMounted(true)
  }, [])

  useEffect(() => {
    if (!mounted) return

    if (user) {
      // 登入後從 Supabase 讀
      const supabase = createClient()
      supabase
        .from('user_answers')
        .select('question_id, date, text_zh, user_answer, answered_at')
        .eq('user_id', user.id)
        .order('answered_at', { ascending: false })
        .then(({ data }) => {
          if (!data) return
          setEntries(
            data.map((row) => ({
              question_id: row.question_id,
              date: row.date,
              text_zh: row.text_zh,
              userAnswer: row.user_answer,
            }))
          )
        })
    } else {
      // 未登入從 localStorage 讀
      setEntries(getHistory())
    }
  }, [user, mounted])

  if (!mounted) {
    return (
      <main style={{ flex: 1, padding: '40px 16px', maxWidth: 600, margin: '0 auto', width: '100%' }}>
        <div style={{ height: 32, background: 'var(--surface-high)', borderRadius: 8, marginBottom: 24, width: 120 }} />
        {[1, 2, 3].map(i => (
          <div key={i} style={{
            height: 80,
            background: 'var(--surface)',
            border: '1px solid var(--border)',
            borderRadius: 12,
            marginBottom: 12,
          }} />
        ))}
      </main>
    )
  }

  return (
    <main style={{ flex: 1, padding: '40px 16px', maxWidth: 600, margin: '0 auto', width: '100%' }}>
      {/* 頁面標題 */}
      <h1 style={{ fontSize: 22, fontWeight: 700, color: 'var(--text)', marginBottom: 6 }}>
        答題紀錄
      </h1>
      <p style={{ fontSize: 13, color: 'var(--text-muted)', marginBottom: 28 }}>
        {user ? '你的跨裝置答題歷史' : '你在這台裝置上的答題歷史'}
      </p>

      {/* 空狀態 */}
      {entries.length === 0 && (
        <div style={{ textAlign: 'center', padding: '60px 0' }}>
          <p style={{ color: 'var(--text-muted)', fontSize: 15, marginBottom: 20 }}>
            還沒有紀錄，去答今天的題目吧
          </p>
          <button
            onClick={() => router.push('/')}
            style={{
              padding: '10px 24px',
              borderRadius: 8,
              border: 'none',
              background: 'var(--teal)',
              color: '#fff',
              fontSize: 14,
              fontWeight: 600,
              cursor: 'pointer',
            }}
          >
            去答題
          </button>
        </div>
      )}

      {/* 歷史列表 */}
      <div style={{ display: 'flex', flexDirection: 'column', gap: 12 }}>
        {entries.map(entry => {
          const color = ANSWER_COLOR[entry.userAnswer] ?? 'var(--teal)'
          return (
            <button
              key={entry.question_id}
              onClick={() => router.push(`/results/${entry.question_id}?answer=${entry.userAnswer}`)}
              style={{
                display: 'flex',
                alignItems: 'center',
                gap: 16,
                padding: '16px 20px',
                background: 'var(--surface)',
                border: '1px solid var(--border)',
                borderRadius: 12,
                cursor: 'pointer',
                textAlign: 'left',
                width: '100%',
                transition: 'border-color 0.15s',
              }}
              onMouseEnter={e => (e.currentTarget.style.borderColor = 'var(--teal)')}
              onMouseLeave={e => (e.currentTarget.style.borderColor = 'var(--border)')}
            >
              <div style={{ flex: 1, minWidth: 0 }}>
                <div style={{ fontSize: 11, color: 'var(--text-muted)', marginBottom: 4, letterSpacing: '0.06em' }}>
                  {entry.date}
                </div>
                <div style={{
                  fontSize: 14,
                  color: 'var(--text)',
                  lineHeight: 1.5,
                  overflow: 'hidden',
                  display: '-webkit-box',
                  WebkitLineClamp: 2,
                  WebkitBoxOrient: 'vertical',
                }}>
                  {entry.text_zh}
                </div>
              </div>
              <div style={{
                width: 36,
                height: 36,
                borderRadius: '50%',
                border: `2px solid ${color}`,
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                fontSize: 14,
                fontWeight: 700,
                color,
                flexShrink: 0,
              }}>
                {entry.userAnswer}
              </div>
            </button>
          )
        })}
      </div>
    </main>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add src/app/history/page.tsx
git commit -m "feat: load history from Supabase when logged in"
```

---

## Task 12：手動驗證

- [ ] **Step 1: 啟動開發伺服器**

```bash
cd human-observatory-web
npm run dev
```

- [ ] **Step 2: 未登入流程**

1. 開啟 `http://localhost:3000`
2. Navbar 顯示「登入」按鈕
3. 答一題送出 → 跳到結果頁
4. 結果頁熱力圖顯示遮罩「登入以查看熱力圖」
5. 前往 `/history` → 看到剛才那筆紀錄（來自 localStorage）

- [ ] **Step 3: 登入流程**

1. 點 Navbar「登入」→ 跳到 Google OAuth
2. 登入成功 → 跳回首頁，Navbar 顯示 email 前綴
3. 答一題送出 → 結果頁熱力圖直接顯示（無遮罩）
4. 在 Supabase console → Table Editor → `user_answers` 確認有新紀錄
5. 前往 `/history` → 顯示「你的跨裝置答題歷史」，有剛才那筆

- [ ] **Step 4: 登出流程**

1. 點「登出」→ Navbar 回到「登入」
2. 結果頁熱力圖遮罩重新出現
3. `/history` 回到 localStorage 資料
