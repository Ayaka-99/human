# 個人頁 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `/profile` 建立個人統計頁，顯示總答題數（英雄橫幅）與最近 12 週答題日曆熱圖，資料全來自 localStorage。

**Architecture:** 純 client-side，`profile/page.tsx` 呼叫 `getHistory()`（已有的 `src/lib/history.ts`）計算統計。日曆邏輯抽出成獨立元件 `ActivityCalendar`，接收 `answeredDates: Set<string>` prop，自行產生最近 84 格。SSR 階段回傳骨架，避免 hydration mismatch。

**Tech Stack:** Next.js 15 App Router, TypeScript, inline styles（跟現有頁面一致），`@/*` path alias

---

## 檔案結構

| 動作 | 路徑 | 說明 |
|------|------|------|
| 新增 | `src/components/charts/ActivityCalendar.tsx` | 12 週日曆熱圖元件 |
| 新增 | `src/app/profile/page.tsx` | 個人頁主頁面 |

---

### Task 1: ActivityCalendar 元件

**Files:**
- Create: `human-observatory-web/src/components/charts/ActivityCalendar.tsx`

- [ ] **Step 1: 建立元件檔案**

```tsx
// human-observatory-web/src/components/charts/ActivityCalendar.tsx
'use client'

interface Props {
  answeredDates: Set<string>
}

/**
 * 產生最近 84 天的格子資料（最舊 → 最新）
 * 使用 sv-SE locale 取得 YYYY-MM-DD（台北時間）
 */
function buildCells(answeredDates: Set<string>): { date: string; filled: boolean }[] {
  const cells: { date: string; filled: boolean }[] = []
  for (let i = 83; i >= 0; i--) {
    const d = new Date()
    d.setDate(d.getDate() - i)
    const date = d.toLocaleDateString('sv-SE', { timeZone: 'Asia/Taipei' })
    cells.push({ date, filled: answeredDates.has(date) })
  }
  return cells
}

export function ActivityCalendar({ answeredDates }: Props) {
  const cells = buildCells(answeredDates)

  return (
    <div
      style={{
        background: 'var(--surface)',
        border: '1px solid var(--border)',
        borderRadius: 12,
        padding: 20,
      }}
    >
      <div style={{ fontSize: 13, fontWeight: 600, color: 'var(--text)', marginBottom: 4 }}>
        答題紀錄
      </div>
      <div style={{ fontSize: 11, color: 'var(--text-muted)', marginBottom: 14 }}>
        最近 12 週
      </div>

      {/* 7 行 × 12 欄，grid-auto-flow: column 讓格子先填滿一週再換欄 */}
      <div
        style={{
          display: 'grid',
          gridTemplateRows: 'repeat(7, 12px)',
          gridTemplateColumns: 'repeat(12, 1fr)',
          gap: 3,
          gridAutoFlow: 'column',
        }}
      >
        {cells.map(({ date, filled }) => (
          <div
            key={date}
            title={date}
            style={{
              borderRadius: 2,
              background: filled ? '#0d9488' : 'var(--surface-high)',
            }}
          />
        ))}
      </div>

      {/* 圖例 */}
      <div
        style={{
          display: 'flex',
          alignItems: 'center',
          gap: 6,
          marginTop: 12,
          justifyContent: 'flex-end',
        }}
      >
        <span style={{ fontSize: 10, color: 'var(--text-muted)' }}>沒答題</span>
        <div
          style={{ width: 10, height: 10, borderRadius: 2, background: 'var(--surface-high)' }}
        />
        <div style={{ width: 10, height: 10, borderRadius: 2, background: '#0d9488' }} />
        <span style={{ fontSize: 10, color: 'var(--text-muted)' }}>有答題</span>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: 確認 TypeScript 無錯誤**

```bash
cd human-observatory-web && npx tsc --noEmit
```

Expected: 無輸出（代表無錯誤）

- [ ] **Step 3: Commit**

```bash
git add human-observatory-web/src/components/charts/ActivityCalendar.tsx
git commit -m "feat: add ActivityCalendar component"
```

---

### Task 2: profile/page.tsx

**Files:**
- Create: `human-observatory-web/src/app/profile/page.tsx`

- [ ] **Step 1: 建立頁面檔案**

```tsx
// human-observatory-web/src/app/profile/page.tsx
'use client'

import { useEffect, useState } from 'react'
import { getHistory } from '@/lib/history'
import { ActivityCalendar } from '@/components/charts/ActivityCalendar'

export default function ProfilePage() {
  const [mounted, setMounted] = useState(false)
  const [total, setTotal] = useState(0)
  const [answeredDates, setAnsweredDates] = useState<Set<string>>(new Set())

  useEffect(() => {
    const history = getHistory()
    setTotal(history.length)
    setAnsweredDates(new Set(history.map((e) => e.date)))
    setMounted(true)
  }, [])

  // SSR 階段回傳骨架，避免 hydration mismatch
  if (!mounted) {
    return (
      <main
        style={{ flex: 1, padding: '40px 16px', maxWidth: 560, margin: '0 auto', width: '100%' }}
      >
        <div
          style={{ height: 120, background: 'var(--surface-high)', borderRadius: 12, marginBottom: 24 }}
        />
        <div style={{ height: 140, background: 'var(--surface)', border: '1px solid var(--border)', borderRadius: 12 }} />
      </main>
    )
  }

  return (
    <main
      style={{ flex: 1, padding: '40px 16px', maxWidth: 560, margin: '0 auto', width: '100%' }}
    >
      {/* 英雄橫幅：總答題數 */}
      <div
        style={{
          background: 'linear-gradient(135deg, #0d9488, #0891b2)',
          borderRadius: 12,
          padding: 28,
          textAlign: 'center',
          marginBottom: 24,
        }}
      >
        <div style={{ fontSize: 48, fontWeight: 700, color: '#fff', lineHeight: 1 }}>
          {total}
        </div>
        <div style={{ fontSize: 13, color: 'rgba(255,255,255,0.75)', marginTop: 6 }}>
          題已完成
        </div>
      </div>

      {/* 答題日曆 */}
      <ActivityCalendar answeredDates={answeredDates} />
    </main>
  )
}
```

- [ ] **Step 2: 確認 TypeScript 無錯誤**

```bash
cd human-observatory-web && npx tsc --noEmit
```

Expected: 無輸出

- [ ] **Step 3: 啟動 dev server 手動驗證**

```bash
cd human-observatory-web && npm run dev
```

瀏覽器開啟 `http://localhost:3000/profile`，確認：
- 英雄橫幅顯示正確答題數（與 `/history` 條目數相符）
- 日曆顯示 12 週格子，有答題日期亮 teal
- 深色 / 淺色主題切換後版面正常

- [ ] **Step 4: Commit**

```bash
git add human-observatory-web/src/app/profile/page.tsx
git commit -m "feat: add profile page with total count and activity calendar"
```
